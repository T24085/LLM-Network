export default function ActivityFeed({logs}:{logs:{event:string;detail:string}[]}){return <div className="lobster-panel h-64 overflow-y-auto p-3"><h3 className="mb-2 text-sm text-brass">Live Activity</h3><ul className="space-y-2 text-xs">{logs.map((l,i)=><li key={i} className="rounded bg-slate-900/60 p-2"><span className="font-semibold text-tube">{l.event}</span> — {l.detail}</li>)}</ul></div>}
