import { AgentStatus } from '../data/mockDashboardData';
import StatusBadge from './StatusBadge';
export default function AgentCard({agent}:{agent:{name:string;role:string;status:AgentStatus;activeTask:string;model:string;tokenUsage:number;uptime:string;health:number;lastAction:string}}){const tone=agent.status==='active'?'ok':agent.status==='idle'?'warn':'bad';return <div className="ocean-card lobster-panel p-4"><div className="mb-2 flex items-center justify-between"><h4 className="font-semibold">{agent.name}</h4><StatusBadge label={agent.status} tone={tone}/></div><p className="text-xs">Role: {agent.role}</p><p className="text-xs">Task: {agent.activeTask}</p><p className="text-xs">Model: {agent.model}</p><p className="text-xs">Tokens: {agent.tokenUsage}</p><p className="text-xs">Uptime: {agent.uptime}</p><p className="text-xs">Health: {agent.health}%</p><p className="text-xs text-slate-400">{agent.lastAction}</p></div>}
